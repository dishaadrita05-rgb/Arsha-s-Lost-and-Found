{
  "name": "Arsha’s Lost and Found (FastAPI)",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "postCreateCommand": "python -m pip install --upgrade pip && python -m pip install -r requirements.txt",
  "postStartCommand": "bash -lc \"nohup python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload >/tmp/uvicorn.log 2>&1 &\"",
  "forwardPorts": [8000],
  "portsAttributes": {
    "8000": {
      "label": "Lost & Found App",
      "onAutoForward": "openPreview"
    }
  }
}
