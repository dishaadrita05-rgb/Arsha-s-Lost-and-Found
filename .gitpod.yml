tasks:
  - init: python -m pip install -U pip && python -m pip install -r requirements.txt
    command: python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

ports:
  - port: 8000
    onOpen: open-preview
